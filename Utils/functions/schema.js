// These functions will act as my schema, simple datachecks
// TODO: add 'jsonschema'
// Checks if an object has all the keys required
checkObject = object => {};

module.exports = {
  checkObject,
};
